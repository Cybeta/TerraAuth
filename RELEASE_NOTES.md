# TerraAuth v0.3.0

> 服务器端权威仿真反作弊服务端（Terraria 1.4.5.8 / 协议 326）
> 发布日期：2026-09-16 ｜ 全量测试：**464 / 464 通过**

本版在 v0.2.0 基础上补齐了**世界生成对齐原版**、**Boss 掉落数据库**、**弹幕 / 增益服务端权威**，
并收口了真机联调暴露的一批缺陷（生命上限口径、挖掘权威、挖砖零掉落、SSC 背包重进清空等）。

---

## 新增功能

### 1. 世界生成对齐原版
- 生成阶段产出**生态群落**：腐化 / 猩红（二选一，同原版）、雪原、沙漠（含地下沙漠沙岩墙）、
  丛林、发光蘑菇、地牢；条带形态改为斜向有机带（噪声驱动，对齐原版 `GERunner` 语义）
- **困难模式地形转换**（血肉墙被击杀时执行一次）：神圣带 + 邪恶带刷新 + 地下群系墙
- **陨石坠落**（落点条件 + 弹坑）与**生命水晶**按原版数量 / 条件放置
- **地表树木**（树干 5–16 格 + 枝条帧表，逐条对照原版 `GrowTree`）
- **生物分支**：`NpcCritterSet`（对齐 `NPCID.Sets.CountsAsCritter`）+ 小动物 AI（地面 / 飞行 + 避让玩家），
  恢复小动物生成池分支

### 2. Boss 掉落升级为原版数据库
- 掉落模型：**概率 + 堆叠范围 + 专家 / 大师袋**；眼魔逐条核对原版掉落库 `RegisterBoss_EOC` 完整建模
  （腐化：恶魔矿 30–90 / 邪箭 20–50 / 腐化种子 1–3；猩红：猩红矿 30–90 / 猩红种子 1–3；
  通用：眼面具 1/7、望远镜 1/40；专家 / 大师：宝袋 3319 必掉）
- 其余 Boss 先以 `Fixed` 包装保持既有行为，避免「顺手改表」引入回归

### 3. 弹幕 / 增益服务端权威
- **NPC 增益同步**：新增包 53（AddNpcBuff）/ 包 54（NpcBuffSync），`WorldNpc.Buffs`（上限 5 槽）
  + 广播链路（全量列表下发，与客户端减益图标一致）
- **服务端弹幕权威仿真**：轴向分离盒式图格碰撞 + 反弹（`BouncesLeft`）+ 追踪转向 + 重力，
  `extraUpdates` 决定每 tick 积分次数；能力表由原版 `Projectile.SetDefaults` 逐类提取
  （408 个穿墙例外 + 228 个 `extraUpdates≥1` + 1130 型碰撞盒），追踪 / 反弹集中收敛
- **非召唤弹幕伤害权威推导**：按手持已收录武器推导伤害并 ±15% 封顶（近战 / 魔法含精确推导，
  远程合并弹药上界），堵掉「虚报高伤弹幕抬命中上界」的作弊面

### 4. NPC 逐类型细节
- 逐类型**重力覆盖**（对齐原版 `UpdateNPC_UpdateGravity`：258、425/427、426、541、576/577 等特例）
- **变体生命上限表**：65 项负 netID 变体的生命上限（对齐客户端 `SetDefaultsFromNetId` 的 `lifeMax = life`）

### 5. 性能与背压
- 快照实体**分包**（`MaxEntitiesPerPacket`）+ `SnapshotStore` 改定长环形缓冲（无元素搬移）
- `CommandQueue` 有界化（生产上限 8192，超限拒绝操作）；分片入站与审计队列改有界
- 握手停滞防护（`HandshakeTimeoutSeconds` 看门狗）；`MetricsEnabled/MetricsPort` 真实接线
- `WorkerPool` 语义收敛（`EnqueueAsync` 等待完成并传播异常）；移除死配置 `MaxWalkSpeed`

---

## 修复

| 现象 | 根因 | 修复 |
|---|---|---|
| 走远再回来 NPC 不动 | 包 23 心跳「只写不读」：`SyncedTick` 每轮无条件推进，判据里没有心跳项 | 变化判据加入 60 tick 心跳；`SyncedTick`/`SyncForced` 仅在确实发出后推进 |
| 史莱姆掉一半血后血条又回到满 | 包 23 不下发 `lifeMax`，客户端按 netID 自算上限（变体有独立上限），服务端用基础类型上限 → 当前生命 > 客户端上限 | 新增变体生命上限表 + 按 netID 取上限；Boss 召唤小怪不再硬编码 20 血 |
| 挖不动地表的砖块 | 包 17 第 5 字段在「挖」时是 `KillTile` 的 **fail 标志**而非图格类型，被当作类型对账 → 草等非 0/1 图格永远判 `tile_type_mismatch` | 挖只校验越界 / 距离 / 图格存在；放砖 / 放墙才校验类型；fail≠0 不改动世界 |
| 挖掉树木 / 地块什么也不掉 | 服务端从未实现原版 `KillTile_DropItems`（客户端本地掉落只写预测槽） | 新增 361 项图格→物品掉落表；砍任意一格树干**整棵倒下**并按格数给木材 |
| 每次重进背包物品全部消失 | SSC 下背包由服务端持有，但运行时档案从未落盘 | 断线按玩家名落盘、新会话进服回读（生命 / 法力 + 59 槽物品与前缀），版本化存档 |
| 打眼魔掉「星辰头盔」 | 掉落表 ID 抄错（`3381` 实为星辰头盔；另有多处同类错误） | 修正为宝袋 `3319`、腐化种子 `59`、眼面具 `2112`、望远镜 `1299`；补猩红种子 `2171` |

---

## 新增配置与命令

| 配置（`server.json`） | 说明 |
|---|---|
| `WorldSeed` | 程序化生成的随机种子，**改种子即换一张新地图**（重启生效） |
| `ResetWorldChangesOnStart` | 换图时清空持久化的图格 / 箱子改动（一次性开关，用完置回 `false`） |
| `VerboseDiagnostics` | 热路径诊断日志（逐包 / 逐次受伤等），默认关闭，支持热重载 |
| `GameMode` | 经典 / 专家 / 大师：影响 Boss 掉落分支与玩家受击公式 |

游戏内命令：`/say` `/who` `/kick` `/help` `/give`，本版新增 **`/boss <npcId> [x y]`**
（调试用，省略坐标则在发起者上方 8 格召唤；生命 / 伤害 / 防御取原版属性表）。

---

## 已知边界（发版时明确，避免误判为缺陷）

- **变体只对齐生命上限**：负 netID 变体的伤害 / 防御仍沿用基础类型（未建模）
- **挖砖掉落**：掉落数量恒为 1；依赖帧 / 随机 / 内含物的图格（草药种子、宝箱内含物、雕像等）未收录；
  树木整棵倒下已实现，但原版按斧力追加木材未建模
- **拾取提示**：聊天行名称为原版**标识名**（如 `Wood`），中文显示名在客户端本地化表内，服务端不提供
- **专家模式**：只影响掉落分支与玩家受击公式，NPC 生命 / 伤害未做 2× 缩放
- **玩家档案**：仅断线时落盘（不做周期保存），增益 / 位置不落盘（位置由宽限期内的会话接管覆盖）
- **世界进度**（已击败 Boss、困难模式等）不落库，进程重启会重置

---

## 使用

```bash
# 解压对应平台的自包含包后直接运行（默认端口 7777，指标端口 9090）
TerraAuth.exe --config server.json --port 7777
```

- 用原版 Terraria 客户端（协议 326）连接即可进入世界：多人游戏 → 加入 → IP:7777
- 常用命令示例：`/give <玩家id> 122`（熔岩镐）、`/boss 4`（眼魔，专家模式必掉宝袋）
- 换图流程：改 `WorldSeed` → 把 `ResetWorldChangesOnStart` 置 `true` 启动一次 → 再置回 `false`
