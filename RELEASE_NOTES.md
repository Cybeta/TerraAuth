# TerraAuth v4.0

> Terraria 1.4.5.8 / 协议 326 的服务端权威仿真服务端
> 发布日期：2026-09-18 ｜ 发布前测试：**710/710 通过**

v4.0 收紧了 SSC 背包、箱子、弹幕、世界改动和持久化链路。原版客户端仍通过协议 326 连接；客户端提交的是受校验的操作意图，最终状态由服务端仿真、持久化和同步链路决定。

## 本版完成

### 库存与箱子

- SSC 下拒绝客户端包 5 的背包最终快照写入，并通过服务端权威槽位纠正客户端。
- SSC 下拒绝客户端包 32 的箱子最终快照写入，避免客户端直接覆盖箱子状态。
- 背包、箱子和拾取使用统一的 `PlayerRuntime` 权威库存账本。
- 实现背包与箱子之间的单槽原子转移，覆盖取出、放入、堆叠合并、部分数量转移和失败回滚。
- 转移校验绑定玩家会话、箱子会话、交互距离、来源快照和操作重放状态。
- 权威库存变更通过同步队列回发原版包 5，并接入玩家档案持久化。

### 弹幕与战斗

- 弹幕实体按 `(Owner, Key)` 隔离，修复不同玩家使用相同 `ProjectileKey` 时的串改风险。
- 已登记弹幕不再接受客户端重复包 27 对位置、速度和活动状态的覆盖。
- 新建弹幕校验有限数值、世界边界、碰撞盒、初始速度和普通远程武器的发射距离。
- 已收录固定武器按服务端手持物品推导允许类型；弹药武器要求服务端库存存在匹配弹药和可确认的弹幕类型。
- 服务端按手持武器、弹药和能力表限制非召唤弹幕伤害上界，并在命中结算时使用权威状态。
- 火箭等最终弹幕类型依赖发射器和原版 `PickAmmo` 规则的武器，不使用不可靠的静态映射放行。

### 世界、掉落与持久化

- 增加图格掉落表，支持树木整棵倒下、木材掉落以及按样式区分的容器类掉落。
- 世界图格、箱子内容、玩家 SSC 背包和档案变更接入持久化读写与回放。
- 增加世界文件写回校验、原子替换和备份滚动支持。
- 增加斧力数据、物品显示名数据和箱子/展示实体基础结构。
- 保留原版客户端协议下的区块、图格、玩家、NPC、弹幕和库存同步链路。

### 测试与工程质量

- 全量测试：710 个用例通过，失败 0，跳过 0。
- 覆盖 SSC 背包拒绝、箱子快照拒绝、库存守恒、箱子转移、持久化、世界文件、弹幕所有权和弹幕生成边界。
- 发布工作流支持 `v*` 标签，并为 Windows/Linux 的 x64 和 arm64 构建自包含包。

## 已知边界

- 原版包 27 不携带一次远程武器使用的事务 ID、实际弹药槽、散射组或消费数量，因此 v4.0 尚未宣称完成所有远程武器的精确服务端弹药扣除。
- 远程武器的完整服务端使用状态机仍需按包 13 控制位建模 `useTime`、`useAnimation`、弹药选择、节省概率、散射和特殊弹药规则；未完成前不能按每个包 27 或每个弹幕简单扣除一发。
- 变体目前主要对齐生命上限；部分伤害、防御和特殊 AI 规则仍沿用基础类型。
- 部分样式、随机、内含物和特殊容器掉落仍未完整覆盖原版规则。
- 玩家档案按断线或会话结束落盘，增益、位置和部分世界进度的持久化范围仍有限，具体以配置和覆盖文档为准。
- 未收录的 Mod 武器、特殊弹幕和复杂弹药规则不会被静默当作普通原版弹幕放行。

## 兼容性

- 原版 Terraria 1.4.5.8 客户端，协议 326。
- 默认端口 7777，指标端口 9090。
- 服务端启动示例：

```bash
TerraAuth.exe --config server.json --port 7777
```

- GitHub Actions 在推送 `v4.0` 标签后自动构建并创建发布包。
