# TerraAuth v4.2

> Terraria 1.4.5.8 / 协议 326 的服务端权威仿真服务端
> 发布日期：2026-09-19

v4.2 延伸服务端对战斗输入、召唤实体和宝藏袋奖励的权威处理。原版客户端仍通过协议 326 连接；客户端提交受校验的操作意图，最终状态由服务端仿真、持久化和同步链路决定。

## 本版更新

### 战斗与资源状态

- 服务端根据玩家持续使用状态按武器 `useTime` / `useAnimation` 节奏生成普通武器弹幕，松开使用后停止生成。
- 服务端开火继续通过弹药、伤害、来源武器和前缀校验，并按权威命令提交，避免仅依赖客户端声明的弹幕结果。
- 召唤 NPC 命中校验绑定已登记的服务端召唤实体；即使当前弹幕位置离开 NPC，也不会因此绕过实体归属检查。
- 召唤 NPC 的命中冷却按实际被命中的实体记录，避免把冷却写入错误的 NPC。

### 宝藏袋与背包

- 支持克苏鲁之眼宝藏袋通过服务端命令开启，奖励生成、背包空间检查和宝藏袋消耗保持原子性。
- 已开启或等待处理的宝藏袋会静默拒绝重复提交及过快的客户端奖励快照，不把正常的时序差异计入违规次数。
- 新增宝藏袋单次开启、背包已满、重复开启、持续使用节奏和召唤命中归属回归测试。

### 交付检查

- 发布配置中的 `VerboseDiagnostics` 保持关闭。
- 本次发布不包含 `debug-summon-hp-resync.md`，也不上传数据库、世界文件、日志或凭据类文件。

## 已知边界

- 完整自动重复开火、多弹幕、特殊弹药和不依赖 Packet 27 的全部服务器主动开火规则仍在实现中。
- Minion/Sentry 尚未完成独立的服务端移动、目标选择、攻击和生命周期模拟。
- 不可变世界检查点、全平台发布包的实际运行验证，以及真实多客户端完整游玩回归仍待完成。

## 兼容性

- 原版 Terraria 1.4.5.8 客户端，协议 326。
- 默认端口 `7777`，指标端口 `9090`。
- 服务端启动示例：

```bash
TerraAuth.exe --config server.json --port 7777
```
