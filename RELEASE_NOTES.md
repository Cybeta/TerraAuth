# TerraAuth v0.6（未发布）

> Terraria 1.4.5.8 / 协议 326 的服务端权威仿真服务端
> 状态：**未发布**（开发中） ｜ 上一版：v0.5.1（标签 `v0.5.1`）

## 本版更新（v0.6，未发布）

### 世界进度落库 + 玩家档案周期保存（修「重启即回档」）
此前「已击败的 Boss、困难模式、事件开关」只存在内存里，**服务端一重启就全部重置**
（打完血肉墙进困难模式，重启退回经典模式）；玩家档案（SSC 背包 / 生命 / 法力）**只在断线时落盘**，
服务端崩溃或被强杀会整局回档。本版把这两条都补上。

- **世界进度落库**：新增 `WorldProgressCodec`（版本化位图：1 字节版本 + 11 字节 = 82 位，表驱动位序），
  把 `WorldProgress` 的全部进度位编成存档；SQLite 增 `WorldProgress` 表（每世界一行，覆盖写），
  启动时回放、1Hz 与停机各落一次、换种子时随世界改动一起清。
  **不复用包 7 的进度字节**：那里混入了 `SscEnabled` / `CloudBgActive` 两个**非进度**字段，
  且 SSC 开关属于进程配置，跟着世界落库会让「改配置重开」得到一个被旧配置污染的世界。
- **档案周期保存**：新增 30 秒周期落盘（并在停机前再补一次全量），修复「崩服丢整局背包」；
  断线仍按原路径即时落盘。周期保存放在网络层，因为档案身份按**玩家名**，而名字只在连接的外观缓存里。
- 落盘判据**不用**「进度脏位」：那个标志是「该给客户端重发包 7 了」的广播信号、会被广播消费清位，
  改与「上次成功落盘的字节」比对（12 字节，成本可忽略），且只有写成功才更新基线（失败自动重试）。

### 其他
- 测试 **881 → 892**（新增进度编解码 82 位逐位往返、损坏输入、落库往返 / 多世界隔离、启动回放闭环等 11 例）。

---

## v0.5.1（已发布，标签 `v0.5.1`）

v0.5.1 是**真机联调缺陷修复（第二轮）**版本：把 v0.5 之后真机复现的问题按「放置链路 → 宝袋开袋 → 诊断可见性」
逐项收口 —— 放置补齐「图格 / 墙 ID → 物品」集合反查、家具类按**图格物件**整体写（尺寸 / 锚点 / 帧）、
被拒时不再吞物品；开袋战利品按**跨全部前缀**的净增判上限、金币上界放宽到真机实测范围。

### ① 放置校验按「图格 / 墙 → 物品集合」反查
放置包携带的是**图格 / 墙 ID**，而背包里是**物品 ID**（工作台：物品 36 ↔ 图格 18；泥土 2 / 0；木头 9 / 30）。
此前只有包 79 走反查、且每个图格只认一个物品，包 17 的放砖 / 放墙既不校验也不扣物品
（客户端放置成功已扣掉本地物品，服务端不扣 = 免费放置，且两边背包不一致）。

- 新增 `TileToItemTable`（图格 524 项 / 墙 203 项，**值为物品 ID 集合**）：同一图格可由数十个物品放置
  （工作台图格 18 有 65 个候选），命中任意一个即可，**扣减优先扣手持槽那件**；
- 包 17 **action 1（放砖）/ action 3（放墙）**接入同一套反查 + 扣减，且与图格写入**同一提交单元**；
- 反查不到（不由任何物品放置的图格）→ `tile_item_unknown`；背包没有 → `item_not_in_inventory`（安全侧）。

### ② 家具类图格按「图格物件」整体放置（修「放下的工作台看起来碎了」）
真机复现：在制作菜单直接制作工作台并放到地图上 —— 客户端本地按整件家具摆好（2 格 + 帧），
服务端却只写「点击的那一格」且不写帧（帧停在本项目的 Empty 标记 `-1,-1`），两边世界立刻分叉：
客户端那件家具看起来碎了，服务端把点击格当成已占用，用户在相邻格再放会被 `tile_already_exists` 拒绝。

- 新增 `TileObjectTable`（自动生成，389 项）：图格 ID → 尺寸 / 锚点偏移 / 样式与贴图坐标参数；
  帧算法与锚点公式均按原版 `TileObject.Place` / `TileObject.CanPlace` 口径
- **放置**：锚点 = 点击格 − `Origin`，按 `Width × Height` 写满整块并写入正确帧；占用检查同样按整块
- **破坏**：多格物件整件移除并只掉 1 份（不再留下半件残格）；普通方块与既有图格实体物件行为不变

### ③ 放置被拒不再销毁物品（守恒层退回无产出凭据的减少）
真机复现：在制作菜单里直接制作一个工作台（成品落在**手持槽**而不是背包），随即放到地图上 ——
客户端本地放置成功后立刻把工作台从手持槽移走并上报包 5，而服务端这次放置**被拒**，
守恒层把这次「只减不增的减少」当纯消耗放行 → **工作台凭空销毁**（地图上也没有新图格）。

修复：权威层拒绝放置时登记「本窗口退回凭据」（候选物品由反查表给出），窗口结算时把这次无产出凭据的
减少**整条摘掉**（权威值原样保留，并回写该槽让客户端拿回物品）。三条判据同时成立才退回：
凭据属于本结算窗口（久置标记不生效）、候选物品已知、该物品本窗口内没有「丢到世界」的凭据
（掉落物已生成 = 这次减少是合法的，退回会凭空多出一份）。

### ④ 开袋战利品不再因「带词缀 / 金币掷骰偏大」被吞
真机复现：右键开袋后**装备与金币又消失**。诊断日志直接指出两处判据误杀：

- **「净增」只统计前缀 0** —— 开袋给的装备可能带词缀（克苏鲁之盾 3097 带前缀 78），
  新到手的那件被算成「净增 0（只是挪位）」而跳过 → 改为**跨全部前缀求和**；
  单次上限仍卡**总净增**（同一件被报成两个前缀、总数 ≥ 2 照样判超限）。
- **金币上界过窄** —— 按原版系数链算出的上界是 2.883 × 基础值，而真机同一袋（3319，基础 30000 铜）
  客户端给出 12 金 30 银 61 铜 = **4.1×**，被判越界回滚 → 上界放宽到 **5×**。
  金币由客户端掷骰，服务端只设上界防止凭空造钱；按理论值收窄会把真机金币吞掉。

### ⑤ 诊断：放置 / 破坏被拒从「看不见」变成「看得见」
- 拒绝日志带上**坐标 + 图格 / 墙 ID**；占用格被拒时还带**该格在服务端权威世界里的实际内容**
  （`现存 tile=… wall=… frame=…`）—— 真机「服务端多出一格」时才能直接看出服务端认为那格是什么。
- 放置 / 破坏类拒绝**始终打印**（此前只打前 50 条，额度早被未建模包占满）。
- 包 17 的挖 / 放（action 0..4）加入 `[Tile]` 明细日志。
- 放置被拒时**补发该格权威单格**（包 20）：客户端本地放置已成功，不补发会在客户端留下「幽灵方块」
  （本地看得见、服务端不认、挖掉也不掉落）。
- `[InventoryTx]` 增加「放置被拒已退回 N 槽」与逐槽退回依据，便于真机核对。

### ⑥ 文档与仓库
- `README`（结构说明里的测试数）、`VANILLA_COVERAGE`（新增「放置（方块 / 墙 / 家具）」与
  「背包守恒事务（SSC）」两行）逐轮同步；全量测试 **843 → 881**。
- v0.5 新增的 `LICENSE` 已按用户要求移除，许可回到 v0.4 口径（README「许可」小节）。

---

## v0.5（已发布，标签 `v0.5`）

v0.5 是**真机联调缺陷修复 + 公开化**版本。真机跑图暴露的一批「物品消失 / 捡不起来 / 开袋异常 / 合成失败」
缺陷在本版收口；同时补齐开袋金币奖励数据、给全部配置项加上中文说明，并把仓库转为公开
（公开前清理了全仓库的本地绝对路径与工具链来源措辞）。

### 掉落物移除改走原版通道（包 151）

- 症状：丢出 / 拾取物品后，地图上留下「能悬停、冒粒子、但永远拾取不了」的物品 —— 也就是「丢出来的东西捡不起来」。
- 根因：服务端用「包 21 + 原 ItemId + stack=0」做移除，而包 21 **没有移除语义** —— 原版客户端 `case 21` 只写 `stack`、
  不动 `type`，失效判定却是 `WorldItem.active => type != 0`；若该槽位在客户端已是空气，该分支还会 `new WorldItem(...)`
  再置 stack=0 → **凭空造出幽灵**。原版移除通道是**包 151（ItemDestroy）**（`NetMessage.SendData` 在 `IsAir` 时
  自动把 21 改写成 151、type 写 0）。
- 修复：掉落物移除广播改用 `PacketId.ItemDestroy`。

### 宝袋（3319）：开袋判定移入守恒事务窗口，战利品与金币按客户端掷骰结算

- 症状①：把袋子**拿在鼠标上**（拖拽 / 换位）→ 袋子**自动被打开**。
  症状②：右键开袋后，战利品与金币**出现约 1 秒后全部消失**。
- 根因：①服务端把「清空袋槽（包 5）」直接当开袋，而鼠标拿起袋子同样会清空该槽，单包粒度无法区分；
  ②开袋奖励由**服务端另掷一次骰**，与客户端本地掷骰（原版 `Player.OpenBossBag` 用 `Main.rand`）写入同一批槽位，
  数值对不上 → 窗口到期判不守恒 → 整窗回滚（战利品被权威值覆盖）。
- 修复：新增数据表 `BossBagLootTable`（21 个宝袋 ID = 原版 `ItemID.Sets.BossBag`；3319 战利品池 8 条 + 单次上限），
  删除「清空袋槽 = 开袋」转换与服务端开袋命令；守恒判据新增宝袋规则：
  **袋净减少 + 合法战利品（池内且不超单次上限）= 开袋提交（客户端掷骰即权威）**；
  仅槽位清空（无战利品 / 无掉落）= 鼠标拿起 → 回滚保袋；丢弃（同窗口有该袋掉落）→ 提交且不复制；
  未建模的 20 个宝袋一律**失败关闭**（回滚保袋，不再被静默销毁）。
- **开袋还给钱**：原版 `Player.OpenBossBag` 结尾把 Boss 的 `NPC.value`（克苏鲁之眼 = 30000 铜币 = 3 金币）
  乘 5 层随机系数后按 1 000 000 / 10 000 / 100 / 1 换算成钱币。本版补上该数据与校验
  （铜币总量须落在 `基础值 × [0.8, 2.883]`），此前金币会被判「凭空造物」而吞掉。
- **开袋独立结算**：袋槽 + 该袋战利品槽先从窗口摘出、按客户端上报值提交，其余槽位照旧各自裁决 ——
  否则背包里只要有一件与服务端不一致的物品，整窗回滚会让**宝袋永远打不开**。

### 合成后立刻放置：不再误拒（工作台在服务端不存在）

- 症状：背包里合成工作台 → 放到地上 → 在它旁边合成**全部失败**（表现为「工作台不能制作物品」）。
- 根因：放置校验只看**权威背包**，而合成要走 15 tick（≈250ms）守恒窗口，权威背包在这段时间还没有该产物 →
  放置被误拒（`item_not_in_inventory`）→ **服务端地图上从来没有这个工作台** → 站位校验（工作台图格 18）失败。
- 修复：`ValidatePlace` 的持有校验改为「权威背包 **+ 本窗口尚未结算的暂存值**」（`HasItemIncludingPending`）；
  `ConsumeItem` 增加从暂存值扣减的回退，避免「校验放过但扣除失败」。

### 放置校验按「图格 → 物品」反查（工作台 / 泥土 / 木头）

- 症状：合成出的工作台放到地上后，旁边合成照旧全部失败（服务端地图上一直没有该图格）—— 与上一节同源，
  但根因更深一层。
- 根因：放置包（包 79 PlaceObject）携带的是**图格 ID**，背包里存的是**物品 ID**，两者并不相等 ——
  工作台 = 物品 36 / 图格 18，泥土 = 物品 2 / 图格 0，木头 = 物品 9 / 图格 30。
  `ValidatePlace` 与 `TilePlaceCommand.Apply` 都拿图格 ID 当物品 ID 用 → 持有校验永远找不到物品
  （`item_not_in_inventory`）→ 放置被拒 → 服务端地图缺该图格 → 之后在它旁边合成因站位缺该图格整窗回滚；
  扣减侧同样扣不到（真机日志：工作台物品 36 明明已在服务端背包，紧随其后的放置仍被拒）。
- 修复：新增自动生成的数据表 `Simulation/Placement/TileToItemTable.cs`（**图格 524 项 / 墙 203 项**，
  数据源为原版 `Item.createTile` / `Item.createWall` 逐条核对；覆盖 `switch (type)` 的 case 分支、
  `if (type == N)` 与 `type >= A && type <= B` 条件链、`DefaultToPlaceableTile` / `DefaultToPlaceableWall`
  三种赋值形态；同一图格可由多个物品放置时取物品 ID 最小者）。`ValidatePlace` 与 `TilePlaceCommand.Apply`
  统一先反查物品 ID 再校验 / 扣减。
- 反查不到（该图格不由任何物品放置，例如树木只靠种子生长）→ **直接拒绝**（安全侧：宁可挡下这一个放置，
  也不允许用任意物品「换」出一个本不该由手持物放置的图格）；命令层同口径，不应用、不扣物品、不改世界。
- 墙条目一并落表备用：墙与原版一致走包 17（`action 3` PlaceWall）而非包 79，本轮不改动放墙链路。
- 回归用例（`Tests/PlacementTests.cs`）：物品 36 放图格 18 接受且扣 1；无物品 → 拒绝 `item_not_in_inventory`；
  手持「物品 18」被拒（证明映射不是恒等）；泥土 0↔2、木头 30↔9 各一条；未收录图格（5）拒绝；
  数据表头条目数与字典一致 + 抽样断言（含墙 1↔26、4↔93）。

### 配置：每一项都有中文说明

- 新增 `Config/ConfigDocumentation.cs`（配置说明唯一来源）：`ConfigurationService.Write` 统一按它渲染，
  因此首次生成配置 / 切换世界 / 重置世界改动时重写 `server.json` 也**不会丢注释**；
  `JsonOptions` 打开 `ReadCommentHandling = Skip`，手写注释的配置照常解析。
- `server.json` 按功能分段逐项注释（网络 / 世界 / 玩家属性 / 移动 / 战斗 / 交互 / 限流 / SSC / 召唤权威 / 违规处置 /
  认证 / Mod / 监控 / 诊断），`ServerConfig` 与 `ModPolicy` 补齐 `///` 中文说明。
- 防回归用例：**任何配置项缺中文说明即测试失败**。

### 公开化：仓库文案审计与清理

- 按既定公开规则审计全仓库文案：**本地绝对路径 0 命中**；**16 处涉及工具链来源的措辞**（均为该次清理之后新增的
  数据表与文档）已全部改为中性表述（「原版行为逐条核对」「由仓库外的一次性生成脚本抽取」）。
- 抽取脚本的具体路径与行号只保留在**仓库外的交接文档**中，仓库内不再出现任何本地工具链痕迹。

### 其他

- 真机诊断：`[InventoryTx]`（背包事务提交 / 回滚 + 暂存 / 权威差异 + 回滚时附合成环境快照）、
  `[ChestTx]`（箱子事务）、`[ItemOwner]`（掉落物归属下发与搜索失败原因）；
  箱子 / 拾取 / 丢弃类拒绝**始终打印**并带细节 JSON（此前只打印前 50 条，被未建模包占满）。
- 仓库主页新增**开发路线**进度展示（5 个节点 + 原版 Boss 立绘）与许可说明。
- 测试：**858 / 858 通过**。

## v0.4.3.2（已发布，标签 `v0.4.3.2`）

v0.4.3.2 是「服务端权威召唤」（backlog `W-2`）的**数据基础 + 前两档**版本：先落四张原版数据表
（本体 / 派生弹幕 / 攻击参数 / 移动参数），再在其上实现 **`ServerDamage` 档**（本体命中的伤害数值由服务端裁定、
命中免疫按原版三档、本体生命周期）与 **`ServerAi` 档**（62 个本体的位置权威 + 命中几何收紧）。

**默认档仍是 `ClientDriven` —— 不开启时行为与 v0.4.3.1 完全一致、零风险**；新档位逐级开启、逐级验证。
（v0.4.3.1 的合成守恒 / 合成环境 / 难度缩放全链见下方「v0.4.3.1（已发布）」小节。）

### 数据基础：四张原版数据表（只落数据、不改行为）

- `Simulation/Combat/SummonEntityTable.cs` —— **召唤 / 哨兵本体 62 条**（46 条由物品 `Item.shoot` 直生 +
  16 条本体变体）。判定本体的唯一权威是原版 `Projectile.SetDefaults` 里赋 `minion = true` / `sentry = true`
  的弹幕类型，物品只作交叉校验。每行带 `aiStyle` / 尺寸 / `timeLeft` / `tileCollide` / 命中免疫三档 + 冷却。
  上一版引用的「27 条」是抽取脚本只扫 2 缩进 `case` 得到的**错误结果**，漏了 19 个物品本体。
- `Simulation/Combat/SummonShotTable.cs` —— 本体发射的**派生弹幕**：32 个本体有派生（34 条关系 / 25 个类型），
  另 30 个本体经完整读 AI 确认无派生。与本体表互补，两者合起来 **62 / 62 全覆盖**。
- `Simulation/Combat/SummonBehaviorTable.cs` —— 32 个会发射本体的**索敌射程 + 攻击间隔**。
  射程度量原版不统一（欧氏 / 曼哈顿 / owner 矩形三种），表里逐条标注。
- `Simulation/Combat/SummonMovementTable.cs` —— 62 个本体的**移动模式 / 召回阈值 / 超远归位 / 速度上限 / 待命点偏移**。
- 四张表都遵守「**抽不到的不猜**」：区分「本 AI 无此机制」与「源码未找到」，逐类型的公式与近似写在注释里并附行号。

### `ServerDamage` 档：本体命中的伤害与节奏收归服务端

- 新增档位配置 `ServerConfig.SummonAuthority`（`ClientDriven`（默认）/ `ServerDamage` / `ServerAi` / `ServerShots`），
  组合根注入 `WorldState.SummonAuthority` 并支持热重载。
- 包 28 对**本体**只作「命中触发」：上报的伤害值不再参与结算，改由服务端按本体登记伤害掷 `DamageVar`（±15%）
  × 暴击位后按 `CalculateDamageNPCsTake` 结算；同时本体**不再作为包 28 的伤害凭据**
  （否则手持任意武器就能拿高伤本体当「上界」）。派生弹幕仍走既有校验口径。
- **命中免疫按原版三档**：默认档（每玩家 10 tick）/ `LocalPerTarget`（每弹幕实例，冷却 3–30）/
  `IdStaticShared`（同 type 共享，10–16）/ `-1`（同一弹幕对同一目标终身一次）/ 星尘龙节段 626-628
  共用头节 625 的免疫数组。此前是硬编码 `Tick + 10`。
- **本体服务端生命周期**：哨兵 `timeLeft = 36000` 到点自毁（`Active=false` + `Destroyed=true` + 包 29）；
  属主离线与「召唤 Buff 消失」两条本就是事件驱动（断线清场 / 包 50），无需轮询。
- 本档**刻意不做服务端重叠判定**：本体位置仍由客户端驱动，原版 minion 的包 27 上报频率不足以保证命中帧
  坐标新鲜，服务端自行求交会**漏伤**。

### `ServerAi` 档：本体位置权威（混合模型）+ 命中几何收紧

- **混合模型**：`ProjectileEntity.ServerPosition` / `ServerVelocity` 是服务端**自算的判定用**位置；
  表现用的 `Position` 仍是客户端包 27 上报值并原样广播（含主人）→ **主人视角无抖动**，客户端无法影响判定。
- 新仿真阶段 `SimulateSummonMovement`：**全部 62 个本体**都拿到服务端自算位置，收敛为四条规则 ——
  贴地族按「主人面朝方向横向排队站位」（AI_026 / AI_067，逐类型基础距离 + 步长入表）、
  飞行 / 悬浮族按「主人中心 + 逐类型待命点偏移」（AI_062 / 54 / 66 / 120 / 121 / 156 / 158 / 164 / 169 / 206）、
  哨兵冻结在生成点、星尘龙节段跟随头节 625 的服务端位置。
- **命中几何收紧**：包 28 的召唤通道对已有服务端位置的本体，要求目标 NPC 落在其 **1200px 可达圈**内，
  否则 `ProjectileNotColliding` —— **堵住「把本体挪到全图任意 NPC 旁再报命中」**（此前任意本体可命中全图任意 NPC）。
- 位置类族另有 `Static`（641 / 643 忽略客户端坐标，服务端持有坐标）与 `PositionBound`
  （831 / 970 的家点、626-628 的父节做 200px 锚点约束）。

### 顺带修掉的缺陷

- **派生弹幕伤害全部丢失**：旧身份集合只收了 389 / 676 / 687 三个派生类型，其余（374 HornetStinger、
  376、378、408、433、614、642、644、664…1120 共 19 个）既不在集合里、`KindOf` 又返回 `None`
  → 玩家手持召唤法杖时包 28 找不到凭据 → 直接 `ProjectileRequired`。现身份集合由数据表算出（本体 ∪ 派生），
  这些弹幕被正确登记。
- **哨兵 / 仆从档位误标**：旧手写 `SentryTypes` 把 831 / 946 / 951 / 970 四个**仆从**标成哨兵，
  又漏掉真哨兵 308 / 377 / 641 / 643 / 663-693 / 966 / 1025。现由本体表回答；
  连带效果是这四个仆从能正确挂上召唤 Buff、Buff 消失时被清理。
- 召唤命中免疫的硬编码冷却（见上）。

### 已知边界（v0.4.3.2）

- **默认档 `ClientDriven` 不含任何新行为**；`ServerDamage` / `ServerAi` 需在 `server.json` 里显式开启。
- **派生弹幕的位置仍不权威**（服务端不持有），因此它们**仍可对全图任意 NPC 报命中**；
  收口需等 `ServerShots` 档由服务端生成它们。
- 服务端**不复刻**索敌 / 冲刺 / 跳跃 / 绕飞等战斗机动；贴地族**不做重力与图块碰撞**
  （读图格需反序取区块锁会死锁），竖直取「主人脚下」近似，横向排队用「同类存活序号」近似原版 `minionPos`。
  这也是命中几何只能给 1200px 可达圈、而非精确重叠的原因。
- **混合模型的「主人视角无抖动」目前只有单元测试背书，尚未真机验证**。

## v0.4.3.1（已发布，标签 `v0.4.3.1`）

v0.4.3.1 是在 v0.4.3 之上的补丁版，把两头补齐：**合成**的守恒判据与**合成站 / 环境校验**（含雪原 / 墓地 /
机械三王 / 火把神恩 4 类环境标志），以及 **NPC 生命与伤害的难度缩放全链**（专家 / 大师 ×2 / ×3、
专家+困难模式弱怪补强、多人按人数放大 Boss 生命，并由包 23 的难度段 / 玩家数段与客户端对齐）。
（v0.4.3 已发布的内容 —— 守恒事务 / 宝袋解耦 / 包 40 / 召唤命中 —— 见下方「v0.4.3（已发布）」小节，
或标签 `v0.4.3` 处的本文件与 GitHub Release。）

## v0.4.3.1 更新

### 合成与消耗：补齐守恒判据（原「唯一硬缺口」）

- 此前守恒判据只看「(物品, 前缀) 总堆叠数不变」，**合成**（材料减少 + 产物增加）与**消耗**（药水 / 投掷物 / 丢弃）都会破坏总量，被当成凭空造物**整窗回滚** —— 表现为合成后物品被吃掉；丢弃物品还会因包 5 被回滚而留下复制缺口（包 21 已生成世界掉落物）。
- 新增原版配方表 `Simulation/Crafting/RecipeTable.cs`（**3301 条配方 + 34 个配方组**，由仓库外的一次性生成脚本从 1.4.5.8 `Recipe.SetupRecipes` 及其家具 / 雕像辅助方法逐条提取，**并复刻原版 `CreateReverseWallRecipes` / `CreateReversePlatformRecipes` 派生的 108 条「墙 → 块 / 平台 → 材料」配方**；静态不可解者整体跳过，绝不落半条配方）。
- 判据升级（`Simulation/Crafting/CraftingConservation.cs`）：净增量若能由配方解释（含配方组按组内任意成员合计扣减）→ **提交**；「只减少、不增加」的纯消耗在**背包侧**放行（箱子侧不放行，防止客户端用包 32 静默清空箱内物品）。
- 安全侧保留：带非 0 前缀的「增加」一律拒绝（合成产物恒为 0 前缀）；消耗上限取窗口开始时的权威总量（新增 `PlayerRuntime.InventoryTransactionBaseline`），窗口期内服务端外部塞入的物品（管理员给予 / 拾取 / 开袋）不会被客户端的清空意图「消耗」掉。
- 开箱期间取用箱内材料合成同理被解释（箱子侧与背包侧一起结算）。
- **合成站 / 环境校验**（新增 `Simulation/Crafting/CraftingEnvironment.cs`）：配方表同时携带原版 `requiredTile`（工作台 / 砧 / 熔炉 / 锯木机…，表内 2943 条带站位）与 `needWater / needHoney / needLava`；
  服务端镜像原版 `Player.AdjTiles()` 采集**可达区域**（X ±5 / Y ±3 图格，玩家碰撞盒 20×42 为基准）内的图格
  （含 `Recipe.TileCountsAs` 等价图格：炼金台算作瓶子等）与相邻液体（`liquid > 200`，含水槽类「算作水源」的图格），
  不满足即回滚 —— 客户端不再能在任意位置合成。
- 已知边界：只校验「材料 → 产物」守恒 + 站位 / 液体，**不校验**依赖客户端分辨率场景度量或玩家解锁状态的标志
  （`needSnowBiome` / `needGraveyardBiome` / `needMechdusa` / `needTorchGodsFavor`）；同一窗口内的多级合成会回滚（安全侧）；
  另有 13 条 Lesion 家具系列配方因依赖原版编译器临时量（`num` / `stack`）静态不可解而整体作废（宁回滚不猜数值）。
  —— 上一句的前半部分**已在同一版本内补齐**，见下节。

### 合成环境：补齐雪原 / 墓地 / 机械三王 / 火把神恩

- 此前配方表里这 4 类环境标志被整体丢弃（雪原 1 条 / 墓地 131 条 / 机械三王 1 条 / 火把神恩 2 条）→ 客户端凑齐材料 + 站合成站旁即可**在任意位置**合成墓碑家具、雪云块、火把药水、机械三王召唤物。
- 核实原版口径：雪原 / 墓地**不依赖客户端分辨率** —— 两者取 `SceneMetrics.ScanTiles()` 的**固定尺寸**扫描区
  （`AssumedConstantScreenSize` 1920×1200 + padding 25 → **169×124 图格**，以玩家所在图格为中心），
  雪原图格 ≥ 1500 → `ZoneSnow`，墓碑数 − 向日葵数/2 ≥ 28 → `ZoneGraveyard`。`CraftingEnvironmentSampler` 在原有可达区域
  扫描内一并采集（可达区域恒被该区域包含），提交时据此校验。
- 机械三王取**世界特性**（原版 `SpecialSeedFeatures.Mechdusa` = remixWorld ∧ getGoodWorld）：本服务端的程序化世界不产出该类种子，故该配方当前世界**不可用**（安全侧）；火把神恩取**玩家解锁状态**（包 4 `TorchFlags` bit2，与客户端一致）。
- 配方表条目数不变（3301 条，其中 135 条带这 4 类标志），生成器文件头改为「已收录 + 判定口径」。

### 难度：NPC 生命与伤害按原版 `ScaleStats` 全链缩放

- 此前难度只影响玩家受击公式的**减防系数**，NPC 的生命上限与伤害仍是经典数值 → 专家 / 大师下怪既不疼也打不死；
  且客户端自算的 NPC 上限（包 23 **不下发 `lifeMax`**）与服务端不一致，血条会发散。
- 对齐原版口径（1.4.5.8，`NPC.ScaleStats` 链）：①**专家 + 困难模式**把强度不足的普通怪补到最低强度线
  （`damage + defense + lifeMax/4` < 80，击败世纪之花后 100，原版用**整数除法**算倍率；Boss / `lifeMax ≥ 1000` /
  `DontDoHardmodeScaling` 类型豁免，投射物型只放大伤害）；②**难度曲线**：生命与伤害按专家 ×2、大师 ×3
  （`EnemyMaxLifeMultiplier` / `EnemyDamageMultiplier` 在经典~大师区间恰好等于难度值本身）；
  ③**在线人数**（仅专家以上）：按 `GetStatScalingFactors` 的 balance（1 / 1.35 / 1.9167…）放大**生命上限**
  （伤害不变），只作用于表内的 Boss 类，撒旦军队按 0.857 插值、火星暴乱 / 旧日军团按 `1 + (n−1)×0.2`。
- `Main.CalculateDamagePlayersTake` 只改减防系数——故把倍率从公式挪到**生成时**（避免双倍放大），
  接触伤害改取 NPC 实体的权威值（未走生成路径的裸实体按「表值 × 倍率」兜底）。
- **包 23 补两段**：难度覆盖段（`bitsB.bit2` + float，缺省 1 是「强覆盖为经典」而非「沿用世界难度」）
  与玩家数段（`bitsB.bit0` + byte，缺省 1 = 单人）。客户端据此用自己的 `SetDefaults` 重算同一个 `lifeMax`，
  血条才与服务端一致（不做这两段，专家 / 大师与多人下血条必然发散）。
- 未建模：`getGoodWorld` 特殊种子分支、`value`（金币数值缩放）、击退抗性削弱、旅途 / 传奇两端难度。

### 文档

- 新增「距原版流畅游玩差距盘点」（交接说明 §8）：按玩家感知分层为 G1–G10，并把其中尚未登记的 6 项落为待办
  （掉落数据库完整化 / 档案周期保存 / `.wld` 段 6..10 / 液体分桶下发 / 多人玩家数缩放 / 专家+困难逐类型微调）——
  末两项已在同一版本内完成（见上「难度」小节），待办表内标注为已完成。

- 按代码逐条核对交接文档，修正 6 处漂移（测试规模、`TileDropTable` 表头条目数 361 → 373、提交号与配方表条目数、
  两处文件路径、`SetDefaults` 抽取形态描述），并新增「表内条目数改了必须同步改文件头」约定。
- 数据表侧：修正 `TileDropTable.cs` 文件头（v0.4.0 增删条目后漏改）。
- **统一测试基线口径**：`README.md` / `PROJECT_STRUCTURE.md` 仍写 779（另一份设计文档写 779/779），实跑为 800
  （补齐本节 4 类环境标志后为 **806**）→ 全部对齐，
  并在 `PROJECT_STRUCTURE.md` §六 记录最近一次基线明细（用例数 / 通过数 / 目标框架 / 配置 / 日期 / 耗时）；
  交接说明为**交接口径的唯一权威来源**，历史轮次记录不回改。

---

## v0.4.3（已发布，标签 `v0.4.3`）

### 背包：守恒事务（客户端可正常整理背包）

- 客户端包 5 与服务端权威槽位不一致时，不再逐包回正，而是暂存进 **15 tick（≈250ms）事务窗口**，窗口到期按 **(物品, 前缀) 总堆叠数守恒**整体提交或回滚：
  - 拖拽 / 交换 / 合并 / 拆分 / 整理都保持总量不变 → **提交**（接受客户端整理结果）；
  - 凭空造物或销毁必然破坏总量 → **回滚**，并把权威值回写客户端。
- 该分流属客户端行为噪声，**不计入违规窗口**（否则正常游玩会被误踢）。
- 与外部变更（管理员给予、拾取、开袋、箱子转移）冲突的暂存意图会因总量对不上而自然回滚，无需额外的版本号或冲突表。
- `SscEnabled=false` 时行为不变：客户端走本地背包，包 5 直接写入。

### 箱子：守恒事务 + 背包↔箱子原子转移 + 快速堆叠

- 包 32 同样改为窗口聚合，守恒基准为**「玩家权威背包 ∪ 该箱子」**的总堆叠数：箱内整理 / 交换守恒即提交，凭空造物即回滚。
- **修复一个会导致玩法不可用的缺陷**：原版在背包与箱子之间拖拽时会同时发出包 5（背包侧清空）与包 32（箱子侧填入）。此前两侧各自做守恒校验，会**把存入箱子的操作全部回滚**（背包侧总量减少、箱子侧总量增加都被判不守恒）。现在开箱期间的包 5 归入同一箱子事务，两侧一起提交或一起回滚。
- 新增包 85（QuickStackChests）入站：客户端上报来源槽位与 smartStack，服务端按当前打开的箱子**自行规划**装箱，不接受客户端提交的最终快照。

### 宝藏袋

- 开袋改为服务端权威命令：奖励随机生成、背包空间检查与袋槽消耗原子完成，重复触发幂等，不会凭空重复产出。
- 移除早期为开袋建立的「全背包静默屏障」：开袋期间其它槽位的正常操作不再被静默丢弃（此前会表现为背包「卡住」）。

### 对话 NPC（包 40）

- 补齐 `SyncTalkNPC`：服务端持有玩家当前对话的城镇 NPC 索引并中继给其他玩家；越界值按同步类记录、**不计违规**，目标不存在或非城镇 NPC 一律回落为「未对话」。

### 召唤物命中

- 命中凭据改为**服务端已登记且存活的召唤实体**，不再要求「缓存位置与 NPC 碰撞盒在当前时刻重叠」——旧判据会让全部命中落空，表现为客户端预测伤害后血条被服务端权威状态「打回满」。
- 归属、弹幕类型、伤害上限、NPC generation 与命中冷却校验全部保留。

### 文档

- 全量核对文档与代码：更新测试规模（779 用例）、协议层路径与包计数、SSC 与箱子守恒语义、Boss 掉落覆盖、NPC 增益与对话同步状态、快照存储实现等。
- 删除 5 份已过时或重复的文档（推送汇总与调试草稿等），合并 3 处重复内容，并清除文档中的本地绝对路径。

---

## 交付检查（v0.4.3.1 发布）

- 自动化测试：全量 **818** 用例通过（v0.4.3 发布时为 779）。
- 发布包仅含程序与配置模板；**不含**数据库、世界文件、日志或凭据类文件（由流水线强制校验）。
- 配置模板中 `VerboseDiagnostics` 保持关闭。

## 已知边界（v0.4.3.1）

- `SscEnabled=true` 时，**合成与纯消耗**已不再被守恒判据回滚：净增量由原版配方表（`Simulation/Crafting/RecipeTable.cs`，3301 条，含派生的「墙 → 块 / 平台 → 材料」）解释，并按 `requiredTile` + 水 / 蜂蜜 / 岩浆**校验合成站与环境**；纯减少（药水 / 投掷物 / 丢弃）直接放行（箱子侧不放行纯消耗）。**未校验**雪原 / 墓地 / 特殊种子 / 火把神恩这类依赖客户端场景度量或玩家解锁状态的标志；同一窗口内的多级合成与 13 条静态不可解的 Lesion 家具配方仍会回滚。
- 完整自动重复开火、多弹幕、特殊弹药，以及不依赖包 27 的全部服务端主动开火规则仍在补全。
- Minion / Sentry 尚未完成独立的服务端移动、目标选择、攻击与生命周期模拟。
  （**该条已在 v0.4.3.2 推进**：本体位置权威与命中几何已覆盖 62 / 62，命中免疫与生命周期已收归服务端；
  目标选择与 `ServerShots` 仍未做。）
- NPC 增益已服务端持有并同步（包 53 / 54），但增益**效果**与计时递减尚未模拟。
- 不可变世界检查点、全平台发布包的实际运行验证，以及真实多客户端完整游玩回归仍待完成。

## 兼容性

- 原版 Terraria 1.4.5.8 客户端，协议 326。
- 默认端口 `7777`，指标端口 `9090`。
- 服务端启动示例：

```bash
TerraAuth.exe --config server.json --port 7777
```
