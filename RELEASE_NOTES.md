# TerraAuth v4.1

> Terraria 1.4.5.8 / 协议 326 的服务端权威仿真服务端
> 发布日期：2026-09-18

v4.1 聚焦服务端权威状态的一致性、世界改动持久化和发布交付检查。原版客户端仍通过协议 326 连接；客户端提交受校验的操作意图，最终状态由服务端仿真、持久化和同步链路决定。

## 本版更新

### 战斗与资源状态

- 已登记武器按服务端 `useTime` 限制开火间隔，并使用有界开火事务历史防止网络重试重复创建弹幕或重复消耗资源。
- 开火提交在同一命令内预检并原子扣除法力和弹药；最后一发弹药被消耗时，弹幕仍保留正确的来源武器和前缀快照。
- 服务端扣除法力后向拥有者单播 Packet 42；其他玩家不会收到该私有状态。
- 会话首次合法法力同步可建立基线；后续客户端上行不能提高已由服务端维护的法力或法力上限，服务端会回写最终值。
- 法力最终状态统一由仿真层维护，避免协议校验层与世界状态之间产生两份可偏离的会话值。
- 弹幕命中要求存在同一拥有者的服务端存活弹幕且与目标碰撞，拒绝没有有效弹幕依据的伤害上报。
- 召唤实体增加稳定内部标识、Minion/Sentry 分类和来源 Buff 关联；移除 Buff 时只清理对应 Minion。

### 世界与持久化

- 多格对象破坏会记录全部受影响图格，避免重启后残留悬空结构。
- 树木和箱子变更覆盖服务端落盘与重启回放回归测试。
- 发布流水线构建前运行全量测试，并同时验证 SQLite 与无 SQLite 后端。
- 发布包检查拒绝数据库、世界文件和日志等运行时数据。

### 交付检查

- 本地验证目录已排除出版本控制。
- 已检查本次待提交源代码、配置、文档和工作流中的常见凭据特征、私钥标记、本地绝对路径及受限术语。

## 已知边界

- 完整 `useAnimation`、自动重复开火、多弹幕、特殊弹药和不依赖 Packet 27 的服务器主动开火仍在实现中。
- Minion/Sentry 尚未完成独立的服务端移动、目标选择、攻击和生命周期模拟。
- 不可变世界检查点、全平台发布包的实际运行验证，以及真实多客户端完整游玩回归仍待完成。

## 兼容性

- 原版 Terraria 1.4.5.8 客户端，协议 326。
- 默认端口 `7777`，指标端口 `9090`。
- 服务端启动示例：

```bash
TerraAuth.exe --config server.json --port 7777
```
